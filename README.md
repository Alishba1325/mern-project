AI Surveillance MERN — Full project
- backend directory: Express + Mongo + JWT auth
- frontend directory: React app with Landing, Login, Signup, Home pages (responsive)
## To run locally:
1. Start MongoDB (e.g., mongod)
2. Backend: cd backend && npm install && copy .env.example to .env && npm run dev
3. Frontend: cd frontend && npm install && npm start
